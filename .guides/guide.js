/*
const commands = {
  openTutorials: function(event) {
    codio.open('preview', 'http://scratch.mit.edu/studios/475476', 1);
  }
}
window.addEventListener('codio-button-custom', function (event) {
  console.log("EVENT", event)
  if(codio) {
    commands[event.cmd](event);
  }
});
*/